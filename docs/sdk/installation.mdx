---
title: "Installation"
description: "Install and configure the Arca React SDK in your project."
---

## Installation

Install the SDK using your favorite package manager:

```bash
npm install arca-react
# or
yarn add arca-react
# or
pnpm add arca-react
```

## Configuration

Wrap your application (usually `App.tsx` or `main.tsx`) with `ArcaProvider` so hooks and components can access your API key.

```tsx
import { ArcaProvider } from "arca-react";

function App() {
  return (
    <ArcaProvider apiKey="arca_...">
      <MyRoutes />
    </ArcaProvider>
  );
}
```

### Provider Props

| Prop | Type | Description |
| :--- | :--- | :--- |
| `apiKey` | `string` | **Required.** Your Arca Developer API Key. |

## Server-side Validation

If you need to verify subscription status securely on your backend (e.g., in Next.js Server Components, API routes, or middleware), please refer to our dedicated [Server-Side Validation](/server-side/validation) guide.

*(Note: UI customization is passed via component props such as `appearance` on `ArcaPricingTable`.)*
